package broTutorials;
/*
Java lambda expressions are Java's first step into functional programming. A Java lambda expression is thus a function
which can be created without belonging to any class. ... Java lambda expressions are commonly used to implement simple
event listeners / callbacks, or in functional programming with the Java Streams API.
Lambda Expressions were added in Java 8.
A lambda expression is a short block of code which takes in parameters and returns a value.
Lambda expressions are similar to methods, but they do not need a name and they can be implemented right in the body of a method.
também conhecido como metodo anónimo (argumentos) -> {codigo}
 */

public class Main {

    public static void main(String[] args) {
/*
        String nome = "Sr.Resende";
        char simbolo = '@';

        MyInterface inicio = (n,s) -> {
            System.out.println("Ola mundo!"+s+"...");
            System.out.println("Está frio hoje...."+n+" !");
        };

        inicio.mensagem(nome,simbolo);

        MyInterface teste2 = (n ,s) -> {
            System.out.println(n+s);
        };

        teste2.mensagem(nome,simbolo);

    }

 */
}
